import React from 'react';
import ReactDOM from 'react-dom';
import AppRoot from './app';

ReactDOM.render(
    <React.StrictMode>
        <AppRoot />
    </React.StrictMode>,
    document.getElementById('test-reactapp-container')
);